/**
 * Configuration options for customizing error messages in Temporal schemas.
 */
export interface TemporalErrorMessage {
  /**
   * Custom message to display when a value is not a valid Temporal object.
   */
  invalid_type?: string
}
